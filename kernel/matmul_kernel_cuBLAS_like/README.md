# cuda matmul kernel cuBLAS-like

The topic touch the kernel math multiplication (mainly CUDA).
This code referenced to [matmul cuBLAS-like](https://siboehm.com/articles/22/CUDA-MMM).

## Setup

```
mkdir build
cd build
cmake ..
make
```
